# Test of smoothing operators
#
# Creates some variables, smooths them, then compares
# the results on varying numbers of processors
#

nout = 10  # No timesteps

MZ = 8    # Z size

dump_format = "nc"  # NetCDF format. Alternative is "pdb"

[mesh]
nx = 10
ny = 3
